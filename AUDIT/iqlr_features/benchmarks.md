# Benchmarks for `iqlr.features()`

## Current benchmarking status

- Not yet benchmarked directly; optimization summary lists it as **MEDIUM** priority.

## Planned scenarios

- Measure selection overhead on `selex`-sized and larger matrices with varying sparsity and zero-inflation.
- Compare performance against alternative implementations that avoid heavy `apply()` usage.

## Output and recording

- Benchmark results and profiling observations to be captured here once `profile.R` is filled in and run.


